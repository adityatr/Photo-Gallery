The sql password and username are the same as mentioned in the test.

The program uses both sql entries and local storage (manually configured to /images)

Front end is based on angularJS, back end is PHP. 

# TODO: Images should have more jpeg
